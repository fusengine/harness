# deps
node_modules/
# build
dist/
*.tsbuildinfo
# generated API docs (run `bun run docs:api`)
docs/api/
# os
.DS_Store
# fuse-lessons dev memory — internal build notes, not package content.
# Root-anchored so it never matches src/memory/ on case-insensitive filesystems.
/MEMORY/
# env
.env
.env.*
!.env.example
